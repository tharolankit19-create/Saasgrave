# ─── Supabase ───────────────────────────────────────────────
# Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_publishable_key
# Server-only. NEVER expose to the browser. Rotate if ever leaked.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_key

# ─── Dodo Payments ──────────────────────────────────────────
# https://app.dodopayments.com → Developer → API Keys
DODO_API_KEY=your_dodo_secret_key
DODO_WEBHOOK_SECRET=your_dodo_webhook_signing_secret
# test_mode | live_mode
DODO_ENV=test_mode
# One fixed-price Dodo product per PRICE POINT — never share a product across
# different prices, or buyers get charged the wrong amount. Listing itself is
# free; opening a listing for sale is $9, and we take 3% when it sells.
DODO_PRODUCT_ID_FEATURED_9=prod_xxxxxxxx     # Featured Launch — $9 / 1 week
DODO_PRODUCT_ID_ADS_29=prod_xxxxxxxx         # Sponsored Row — $29 / 1 month
# The $49 price point, shared by the two $49 placements:
# Sidebar Slot and Newsletter Mention (both / 1 month).
DODO_PRODUCT_ID_ADS_49=prod_xxxxxxxx
DODO_PRODUCT_ID_DIRECTORY_99=prod_xxxxxxxx   # Directory Blast — $99 one-off
DODO_PRODUCT_ID_BUNDLE_149=prod_xxxxxxxx     # Everything Bundle — $149
DODO_PRODUCT_ID_SALE_9=prod_xxxxxxxx         # Open a listing for sale — $9 one-off

# ─── AI listing autofill (optional) ─────────────────────────
# Lets founders paste their site's URL and have the listing filled in.
# Firecrawl reads the page (https://firecrawl.dev); the AI keys below turn it
# into listing fields — set OPENROUTER_API_KEY or GEMINI_API_KEY for that.
FIRECRAWL_API_KEY=

# ─── Analytics (optional) ───────────────────────────────────
# Powers the live "visitors this week" FOMO counter on the landing page.
# Plausible: Settings → API Keys (a stats-api key), and your site's domain.
# Leave blank and the counter falls back to real on-platform activity.
PLAUSIBLE_API_KEY=
PLAUSIBLE_SITE_ID=your-domain.com
# Self-hosted Plausible? point this at it. Defaults to plausible.io.
PLAUSIBLE_HOST=https://plausible.io

# ─── AI (story mode) ────────────────────────────────────────
# Preferred: OpenRouter (works with NVIDIA Nemotron and many models).
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_key
# Optional — any OpenRouter model id. Defaults to an NVIDIA Nemotron model.
OPENROUTER_MODEL=nvidia/llama-3.1-nemotron-70b-instruct
# Fallback provider (used only if OPENROUTER_API_KEY is unset).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash-latest

# ─── App ────────────────────────────────────────────────────
# Local dev: http://localhost:3000 · Production: https://saasgrave.org
NEXT_PUBLIC_SITE_URL=https://saasgrave.org
